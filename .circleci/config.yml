version: 2
jobs:
  build:
    docker:
      - image: circleci/buildpack-deps:stretch-curl
    working_directory: ~/rails5
    steps:
      - setup_remote_docker:
          version: 18.09.3
      - checkout
      - run:
          name: Test Command
          command: |
            ls -las
      - run:
          name: Build Project
          command: |
            docker-compose build
      - run:
          name: Verify Docker images
          command: |
            docker images
      - run:
          name: Run Docker containers
          command: |
            docker-compose up -d
      - run:
          name: Wait for containers
          command: |
            dockerize -wait tcp://localhost:3306 -timeout 20s
      - run:
          name: Setup Databases
          command: |
            docker-compose exec app rake db:create db:migrate
            docker-compose run app rake db:create db:create

## Example for machine executor

#version: 2
#jobs:
#  build:
#    machine: true
#    working_directory: ~/rails5
#    steps:
#      - checkout
#      - run:
#          name: Test Command
#          command: |
#            ls -las
#      - run:
#          name: Build Project
#          command: |
#            docker-compose build
#      - run:
#          name: Verify Docker images
#          command: |
#            docker images